# EXAMPLE / STARTER TEMPLATE - real, loadable, but named "example-" on
# purpose: delete or rename this before adding real requirements here.
# See regulatory_content/README.md for the full field reference. `key`
# is this Requirement's stable identity across reloads (becomes
# Requirement.human_reference) - pick it once, keep it even if you
# rename this file later. Each entry in `rules:` is a Rule that
# enforces this obligation; each also gets its own stable `key`
# (defaulted to "<this key>__rule-<index>" if you don't set one).

key: example-beauty-claims-prohibited-therapeutic-claim
jurisdiction: Malaysia
category: Beauty
dimension: CLAIMS
authority: "National Pharmaceutical Regulatory Agency (NPRA)"
authority_interpretation_label: AUTHORITY_REQUIREMENT
obligation_type: PROHIBITED_THERAPEUTIC_CLAIM
canonical_statement: >
  Cosmetic claims must not assert a therapeutic or medicinal effect.
default_severity: CRITICAL
is_hard_gate: true

rules:
  - key: my-beauty-claims-prohibited-wording-check
    output_type: FINDING_PROPOSAL
    unknown_behavior: HUMAN_REVIEW
    condition:
      op: in
      field: wording
      normalize: lowercase
      value:
        - cures acne
        - treats eczema
